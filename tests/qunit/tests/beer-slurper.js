/**
 * @file beer-slurper.js
 * @description QUnit test suite for the Beer Slurper WordPress plugin.
 *              Contains JavaScript unit tests to validate plugin functionality.
 */

/**
 * Basic sanity test for the QUnit test framework.
 *
 * @description Tests that the QUnit test framework is properly configured and running.
 *              Validates JavaScript type coercion by asserting that the number 1 equals
 *              the string "1" using loose equality comparison. This serves as a
 *              placeholder test to verify the test infrastructure is operational.
 */
test( "hello test", function() {
  ok( 1 == "1", "Passed!" );
});
